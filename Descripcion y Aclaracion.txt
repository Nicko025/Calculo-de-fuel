Es un logaritmo sencillo de una regla de 3 simples para hacerel calculo de fuel en carreras de endurance (terminan por tiempo y no por vueltas).
Se le solicita al usuario que se ingrese valores de: tiempo de vuelta, consumo por vuelta y duracion total de la carrera, con lo cual indica la cantidad de fuel total que se debera cargar en el auto mas un extra de una vuelta por una eventual contingencia.

AVISO:

#El programa estaba hecho con imagenes y sonidos, los cuales se encuentran anulados con un doble ##,
 en caso de querer usarse se deberan sacarse dichos caracteres ademas de poner imagenes y sonidos en la misma carpeta donde se encuentre el main.py
 alterando el nombre dentro de los archivos de destino (.wav y .png) dentro del codigo.
 
#En caso de querer agregar una imagen, el programa le dara un size de 470x593, este tamaño se puede sacar o modificar en la linea: 100
 
 
#los modulos usados fueron:
  tkinter:
  pip install tk
  
  winsound:
  pip install beeply
  
  pillow:
  pip install pillow
  
  random*:
  *solo funciona en caso de que inserten sonidos, su funcion es hacer que cada vez que se oprima un boton el sonido sea distinto para no volverl tan monotono.
  
  
  ***de cumplirse todos estos requisitos con los modulos el archivo .py es totalmente compatible para uan conversion a un ejecutable .exe***
 
